<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="cliant.Flyer">
    <ContentPage.Content>
        <ListView x:Name="FlyerList" HasUnevenRows="true">
            <ListView.ItemTemplate>
                <DataTemplate>

                    <ViewCell>
                        <StackLayout
                        HorizontalOptions="FillAndExpand"
                        Orientation="Horizontal">
                            <StackLayout
                            Orientation="Vertical">
                                <Label Text="{Binding Title}" TextColor="#000000" />
                                <Label Text="{Binding Date}" TextColor="#000000" />
                                <Label Text="{Binding Categoly}" TextColor="#000000" />
                                <Label Text="{Binding Place}" TextColor="#000000"
                                <Label Text="{Binding Text}" TextColor="#000000" />
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>